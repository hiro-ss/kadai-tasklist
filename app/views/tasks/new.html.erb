<h1>タスク新規作成ページ</h1>

<%= render "form", task: @task %>

<%= link_to "一覧に戻る",tasks_path %>

<% if @task.errors.any? %>
      <ul>
        <% @task.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
<% end %>
